<div class="row">
    <div class='col-md-8 col-md-offset-2 well'>
        <%= form_for @style do |f| %>
            <div class="form-group">
                 Name: <%= f.text_field :name, class:"form-control" %><br />
            </div>
              <%= f.submit "Add New Style", class:"btn btn-warning" %>
              <%= link_to "Cancel", :back, class:"btn btn-danger" %>
        <% end %>
    </div>
</div>